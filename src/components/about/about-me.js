import React from 'react';
// import PropTypes from 'prop-types';

const BioContainer = () => (
  <main className="bio__main-container" data-test-bio-container>
    is the about container<p>hello</p>
  </main>
);

export default BioContainer;
